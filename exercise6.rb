#get error because there if..else needs to be closed with an "end"
#to fix code, add "end" before the other "end"

 def equal_to_four(x)
      if x == 4
        puts "yup"
      else
        puts "nope"
    end
  end

equal_to_four(5)